# theme-toggle-menu

**Branch:** claude/add-theme-toggle-menu-MOJsD
**Base branch:** main
**Stack:** Vanilla HTML, CSS, JavaScript (no build tools, no framework)
**Run tests:** n/a
**Run locally:** Open index.html in browser
**Key conventions:** All styles in m8lset.css; all JS in m8lset.js; CSS custom properties (var(--*)) for theming; theme controlled via data-theme="dark" on <body>
